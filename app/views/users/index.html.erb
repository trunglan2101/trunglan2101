<% provide(:title, 'All Users Microposts') %>
<%# <h1>All users</h1> %>
<%# 
<%# <%= will_paginate @users %>
<%# <ul class="users "> %>
  <%# <% @users.each do |user| %>
    <%# <%= render user %>
  <%# <% end %>
<%# </ul> %>
<%# <%= will_paginate @users %>

<%# <%= will_paginate @microposts %>
<%# <ul> %>
  <%# <% @microposts.each do |microposts|%>
    <%# <%= render microposts%>
  <%# <% end %>
<%# </ul> %>
<%# <%= will_paginate @microposts %>
<%# <%= will_paginate @users %>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Stt</th>
      <th scope="col">Avatar</th>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
  
    <% @users.each_with_index do |user, index| %>
      
      <%= render "user_item", user_item: user, index: index %>
      
    <% end %>
    
  </tbody>
</table>
<div>
  <%= link_to "Create new user", signup_path , class:"btn btn-secondary"%>
</div>
<%= will_paginate @user %>